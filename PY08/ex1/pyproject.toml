[tool.poetry]
name = "matrix-loading"
version = "1.0.0"
description = "Matrix data analysis and dependency management"
authors = ["Your Name"]

[tool.poetry.dependencies]
python = "^3.11"
pandas = "^2.0"
numpy = "^1.24"
matplotlib = "^3.7"

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"